import { InjectionToken} from '@angular/core';

export const LocatStorageToken =  new InjectionToken<any>('local storage', {
  providedIn: 'root',
  factory() {
      return sessionStorage;
  },

});
